using DatabaseConnection;
using System.Threading;

namespace Program
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
            SqlDatabase.OpenConnection(null, new System.TimeSpan(0, 0, 1));

            SqlDatabase.OpenConnection("321", new System.TimeSpan(0,0,1));
            System.Console.WriteLine("GetDbConnectionStatus: " + SqlDatabase.GetDbConnectionStatus()); 
            SqlDatabase.CloseConnection();

            System.Console.WriteLine("\n**********\n");

            SqlDatabase.OpenConnection("123", new System.TimeSpan(0, 0, 1));
            System.Console.WriteLine("GetDbConnectionStatus: " + SqlDatabase.GetDbConnectionStatus());
            SqlDatabase.CloseConnection();
            System.Console.WriteLine("GetDbConnectionStatus: " + SqlDatabase.GetDbConnectionStatus());


            System.Console.WriteLine("\n**********\n");

            var OracleDatabase = new OracleConnection("123");
            OracleDatabase.OpenConnection("321", new System.TimeSpan(0, 0, 1));
            System.Console.WriteLine("GetDbConnectionStatus: " + OracleDatabase.GetDbConnectionStatus());
            OracleDatabase.CloseConnection();
            
            System.Console.WriteLine("\n**********\n");

            OracleDatabase.OpenConnection("123", new System.TimeSpan(0, 0, 1));
            System.Console.WriteLine("GetDbConnectionStatus: " + OracleDatabase.GetDbConnectionStatus());
            OracleDatabase.CloseConnection();
            System.Console.WriteLine("GetDbConnectionStatus: " + OracleDatabase.GetDbConnectionStatus());
            */
        }
    }
}
